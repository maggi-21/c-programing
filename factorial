#include <stdio.h>
int fact(int n);
void main() {
    int n,ans;
    printf("enter a number");
    scanf("%d",&n);
   ans=fact(n);
    
    printf("%d",ans);
}
int fact(int n)
{
    if(n==0)
    return 1;
    else
    return (n*fact(n-1));
}
output:
enter a number5
120
