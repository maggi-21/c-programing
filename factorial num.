#include <stdio.h>

int main() {
int a[]={1,2,3,4};
int len=sizeof(a)/sizeof(a[0]);
int i,n,fact=1,sum=0;
for(i=0;i<len;i++)
{
 n=a[i];
 for(i=1;i<=n;i++)
 {
 fact=fact*i;
 sum=sum+fact;
 }
}
printf("%d",sum);
    return 0;
}

    

